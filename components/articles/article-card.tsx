import Image from "next/image";
import Link from "next/link";
import { Eye, MessageCircle } from "lucide-react";
import type { Article } from "@/lib/blogger/types";
export function ArticleCard({ article, compact = false }: { article: Article; compact?: boolean }) { return <article className="group paper overflow-hidden rounded-2xl transition duration-300 hover:-translate-y-1 hover:shadow-lg"><Link href={article.url} className="block"><div className={compact ? "relative h-34" : "relative h-48"}><Image src={article.cover} alt="" fill className="object-cover transition duration-500 group-hover:scale-105" sizes="(max-width: 768px) 100vw, 420px" /></div><div className="p-5"><p className="mb-2 text-[10px] font-bold uppercase tracking-wide text-orange-500">{article.labels[0]} <span className="text-zinc-400">• {new Date(article.published).toLocaleDateString("en-US", { month: "short", day: "numeric" })}</span></p><h3 className="line-clamp-2 text-lg font-bold leading-tight tracking-tight">{article.title}</h3><p className="mt-3 line-clamp-2 text-xs leading-relaxed text-zinc-500">{article.excerpt}</p><div className="mt-5 flex items-center justify-between border-t border-zinc-100 pt-3 text-[10px] text-zinc-400 dark:border-zinc-800"><span>{article.author.name} · {article.views?.toLocaleString()} views</span><span className="flex gap-2"><Eye className="size-3" /> <MessageCircle className="size-3" /> {article.comments}</span></div></div></Link></article> }
