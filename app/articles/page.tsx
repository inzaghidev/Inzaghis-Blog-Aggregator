import { getArticles } from "@/lib/blogger/service";
import { ArticleCard } from "@/components/articles/article-card";
export default async function Articles({ searchParams }: { searchParams: Promise<{ q?: string }> }) { const q = (await searchParams).q; const articles = await getArticles(q); return <main className="mx-auto max-w-7xl px-4 py-12 sm:px-6"><p className="text-xs font-bold uppercase tracking-wider text-orange-500">All stories</p><h1 className="mt-2 text-4xl font-extrabold tracking-tight">{q ? `Results for “${q}”` : "Ideas worth building"}</h1><form className="mt-7"><input name="q" defaultValue={q} placeholder="Search articles…" className="w-full max-w-lg rounded-xl border border-zinc-200 bg-white px-4 py-3 text-sm outline-none focus:border-orange-500 dark:border-zinc-800 dark:bg-zinc-900"/></form><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{articles.map(a => <ArticleCard key={a.id} article={a}/>)}</div>{!articles.length && <p className="mt-12 text-zinc-500">No stories matched your search.</p>}</main> }
